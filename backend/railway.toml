[build]
builder = "nixpacks"

[deploy]
startCommand = "java -Xms256m -Xmx768m -jar target/anicon-backend-1.0.0.jar"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
